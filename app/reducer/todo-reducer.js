const initialState = ['comprar la leche', 'comprar los huevos', 'comida pa los loritos'];

const toDosReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default toDosReducer;
